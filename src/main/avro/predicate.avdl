@namespace("syndeticlogic.catena.avro")
protocol Predicate {
   
   enum Type {
     Type,
     Boolean,
     Byte,
     Character,
     Short,
     Integer,
     Long,
     Float,
     Double,
     String,
     Binary,
     Codeable
   }

  record Predicate {
    bytes encodedPredicate;
  }
}